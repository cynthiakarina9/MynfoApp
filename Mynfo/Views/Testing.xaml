<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Mynfo.Views.Testing">
    <NavigationPage.TitleView>
        <Grid 
            Padding = "0" 
            BackgroundColor = "Transparent">
           <!-- <ImageButton
                BackgroundColor = "Transparent"
                HorizontalOptions = "Start"
                VerticalOptions = "Center"
                WidthRequest = "50"
                Source="back"
                x:Name="BackButton"/>-->
        </Grid>
    </NavigationPage.TitleView>
    <ContentPage.Content>
        <StackLayout>
            <!--<Button Text="Hola mundo" x:Name="AbrirPopUp"/>-->
            <Label 
                FontSize = "30" 
                FontAttributes="Bold"
                HorizontalOptions = "Center"
                VerticalOptions = "Center"
                TextColor="Red"
                Text="Nom box"
                x:Name="boxLocalName"/>
            <Label 
                Padding="0,25,0,0"
                FontSize = "15" 
                HorizontalOptions = "Center"
                VerticalOptions = "Center"
                x:Name="networksLocalNames"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>